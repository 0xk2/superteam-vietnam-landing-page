// Every Build Station page and API lives under this path on vn.superteam.fun.
export const BASE = '/danang-buildstation'

export const PAGES = {
  home: `${BASE}/`,
  learn: `${BASE}/learn/`,
  projects: `${BASE}/projects/`,
  submit: `${BASE}/submit/`,
  contact: `${BASE}/contact/`,
}

export const API = {
  projects: `${BASE}/api/projects`,
  submissions: `${BASE}/api/submissions`,
}
